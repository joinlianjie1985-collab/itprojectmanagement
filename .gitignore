# macOS
.DS_Store

# Secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa*
.netrc
.npmrc

# Local build output (the Pages workflow builds _site on the runner)
_site/

# Editor scratch
*.swp
*~

# Playwright MCP scratch output
.playwright-mcp/

# Third-party skills — restore with: npx skills experimental_install
# (kept out of git; skills-lock.json pins source + content hash)
.claude/skills/
